# Invoking lambdas from other lambdas with `serverless-offline@6`

```bash
$ npm start

$ npm run invoke:[event|reqres]:[function|http]
```

- invoking with `serverless invoke` works as expected
- invoking with `curl` works as expected
